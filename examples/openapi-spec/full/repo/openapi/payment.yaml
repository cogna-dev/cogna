openapi: 3.1.0
info:
  title: Payment API
  version: 1.0.0
paths:
  /payments:
    get:
      operationId: listPayments
      responses:
        "200":
          description: ok
components:
  schemas:
    Payment:
      type: object
      properties:
        id:
          type: string
