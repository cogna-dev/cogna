// Code generated by mapi. DO NOT EDIT.
///|
pub(all) struct ApiTransport {
  send : (@lib.RequestEnvelope) -> Result[@lib.ResponseEnvelope, @lib.AppError]
}

///|
pub(all) struct ApiClient {
  transport : ApiTransport
  base_path : String
  default_headers : Map[String, String]
}

///|
pub fn ApiClient::new(
  transport : ApiTransport,
  base_path~ : String = "",
  default_headers~ : Map[String, String] = Map::new(),
) -> ApiClient {
  { transport, base_path, default_headers }
}

///|
fn copy_headers(source : Map[String, String]) -> Map[String, String] {
  let headers = Map::new()
  for key, value in source {
    headers[key] = value
  }
  headers
}

///|
fn join_paths(base_path : String, route_path : String) -> String {
  if base_path.is_empty() {
    route_path
  } else if route_path.is_empty() {
    base_path
  } else if base_path.has_suffix("/") && route_path.has_prefix("/") {
    base_path + route_path.sub(start=1).to_string()
  } else if !base_path.has_suffix("/") && !route_path.has_prefix("/") {
    base_path + "/" + route_path
  } else {
    base_path + route_path
  }
}

///|
fn app_error_from_status(status : Int) -> @lib.AppError {
  match status {
    400 => @lib.AppError::BadRequest("Remote service returned 400")
    401 => @lib.AppError::Unauthorized
    403 => @lib.AppError::Forbidden
    404 => @lib.AppError::NotFound("Remote service returned 404")
    _ => @lib.AppError::Internal("Remote service returned unexpected status: " + status.to_string())
  }
}

///|
pub fn ApiClient::health_check(
  self : ApiClient,
  _input : HealthCheckInput,
) -> Result[HealthCheckResponse, @lib.AppError] {
  let headers = copy_headers(self.default_headers)
  let query : Map[String, Array[String]] = Map::new()
  let route_path = "/health"
  let body : Bytes? = None
  let req : @lib.RequestEnvelope = {
    http_method: @lib.HttpMethod::Get,
    path: join_paths(self.base_path, route_path),
    query,
    headers,
    body,
  }
  let response = match (self.transport.send)(req) {
    Ok(resp) => resp
    Err(err) => return Err(err)
  }
  if response.status != 200 {
    return Err(app_error_from_status(response.status))
  }
  Ok(HealthCheckResponse::Ok(()))
}

///|
pub fn ApiClient::get_object(
  self : ApiClient,
  input : GetObjectInput,
) -> Result[GetObjectResponse, @lib.AppError] {
  let headers = copy_headers(self.default_headers)
  let query : Map[String, Array[String]] = Map::new()
  let route_path = "/cache/v1/object"
  query["key"] = [input.key]
  let body : Bytes? = None
  let req : @lib.RequestEnvelope = {
    http_method: @lib.HttpMethod::Get,
    path: join_paths(self.base_path, route_path),
    query,
    headers,
    body,
  }
  let response = match (self.transport.send)(req) {
    Ok(resp) => resp
    Err(err) => return Err(err)
  }
  if response.status != 200 {
    return Err(app_error_from_status(response.status))
  }
  Ok(GetObjectResponse::Ok(()))
}

///|
pub fn ApiClient::put_object(
  self : ApiClient,
  input : PutObjectInput,
) -> Result[PutObjectResponse, @lib.AppError] {
  let headers = copy_headers(self.default_headers)
  let query : Map[String, Array[String]] = Map::new()
  let route_path = "/cache/v1/object"
  query["key"] = [input.key]
  let body : Bytes? = None
  let req : @lib.RequestEnvelope = {
    http_method: @lib.HttpMethod::Put,
    path: join_paths(self.base_path, route_path),
    query,
    headers,
    body,
  }
  let response = match (self.transport.send)(req) {
    Ok(resp) => resp
    Err(err) => return Err(err)
  }
  if response.status != 201 {
    return Err(app_error_from_status(response.status))
  }
  Ok(PutObjectResponse::Ok(()))
}

