// Code generated by protoc-gen-cogna v0.2.0. DO NOT EDIT.
// Source: xaclabs/cogna/v1/diff.proto

///|
pub(all) struct DiffRequest {
  base : String?
  target : String?
  include_test_changes : Bool?
} derive(Eq)

///|
pub fn DiffRequest::to_json(self : DiffRequest) -> Json {
  let obj : Map[String, Json] = {}
  match self.base {
    Some(value) => obj["base"] = Json::string(value)
    None => ()
  }
  match self.target {
    Some(value) => obj["target"] = Json::string(value)
    None => ()
  }
  match self.include_test_changes {
    Some(value) => obj["include_test_changes"] = Json::boolean(value)
    None => ()
  }
  Json::object(obj)
}

///|
pub fn DiffRequest::from_json(j : Json) -> DiffRequest? {
  guard j is Object(obj) else { return None }
  let base : String? = match obj.get("base") {
    Some(value) =>
      match value {
        String(text) => Some(text)
        _ => None
      }
    None => None
  }
  let target : String? = match obj.get("target") {
    Some(value) =>
      match value {
        String(text) => Some(text)
        _ => None
      }
    None => None
  }
  let include_test_changes : Bool? = match obj.get("include_test_changes") {
    Some(value) =>
      match value {
        True => Some(true)
        False => Some(false)
        _ => None
      }
    None =>
      match obj.get("includeTestChanges") {
        Some(value) =>
          match value {
            True => Some(true)
            False => Some(false)
            _ => None
          }
        None => None
      }
  }
  Some({ base, target, include_test_changes })
}

///|
pub fn DiffRequest::to_json_string(
  self : DiffRequest,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn DiffRequest::from_json_string(s : String) -> Result[DiffRequest, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match DiffRequest::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}

///|
pub(all) struct DiffResponse {
  summary : DiffChangeSummary?
  changes : Array[DiffChange]
  test_changes : Array[DiffChange]
} derive(Eq)

///|
pub fn DiffResponse::to_json(self : DiffResponse) -> Json {
  let obj : Map[String, Json] = {}
  match self.summary {
    Some(value) => obj["summary"] = value.to_json()
    None => ()
  }
  obj["changes"] = Json::array(self.changes.map(fn(item) { item.to_json() }))
  obj["testChanges"] = Json::array(
    self.test_changes.map(fn(item) { item.to_json() }),
  )
  Json::object(obj)
}

///|
pub fn DiffResponse::from_json(j : Json) -> DiffResponse? {
  guard j is Object(obj) else { return None }
  let summary : DiffChangeSummary? = match obj.get("summary") {
    Some(value) => DiffChangeSummary::from_json(value)
    None => None
  }
  let changes : Array[DiffChange] = match obj.get("changes") {
    Some(value) =>
      match
        (match value {
          Array(items) => {
            let parsed : Array[DiffChange] = []
            for item in items {
              match DiffChange::from_json(item) {
                Some(value) => parsed.push(value)
                None => return None
              }
            }
            Some(parsed)
          }
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => []
  }
  let test_changes : Array[DiffChange] = match obj.get("testChanges") {
    Some(value) =>
      match
        (match value {
          Array(items) => {
            let parsed : Array[DiffChange] = []
            for item in items {
              match DiffChange::from_json(item) {
                Some(value) => parsed.push(value)
                None => return None
              }
            }
            Some(parsed)
          }
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => []
  }
  Some({ summary, changes, test_changes })
}

///|
pub fn DiffResponse::to_json_string(
  self : DiffResponse,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn DiffResponse::from_json_string(
  s : String,
) -> Result[DiffResponse, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match DiffResponse::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}
