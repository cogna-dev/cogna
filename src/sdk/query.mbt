// Code generated by protoc-gen-cogna v0.2.0. DO NOT EDIT.
// Source: xaclabs/cogna/v1/query.proto

///|
pub(all) struct FetchPackagesRequest {} derive(Eq)

///|
pub fn FetchPackagesRequest::to_json(_self : FetchPackagesRequest) -> Json {
  Json::object({})
}

///|
pub fn FetchPackagesRequest::from_json(j : Json) -> FetchPackagesRequest? {
  guard j is Object(_) else { return None }
  Some(FetchPackagesRequest::{  })
}

///|
pub fn FetchPackagesRequest::to_json_string(
  self : FetchPackagesRequest,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn FetchPackagesRequest::from_json_string(
  s : String,
) -> Result[FetchPackagesRequest, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match FetchPackagesRequest::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}

///|
pub(all) struct FetchPackagesResponse {
  root : PackageNode?
} derive(Eq)

///|
pub fn FetchPackagesResponse::to_json(self : FetchPackagesResponse) -> Json {
  let obj : Map[String, Json] = {}
  match self.root {
    Some(value) => obj["root"] = value.to_json()
    None => ()
  }
  Json::object(obj)
}

///|
pub fn FetchPackagesResponse::from_json(j : Json) -> FetchPackagesResponse? {
  guard j is Object(obj) else { return None }
  let root : PackageNode? = match obj.get("root") {
    Some(value) => PackageNode::from_json(value)
    None => None
  }
  Some({ root, })
}

///|
pub fn FetchPackagesResponse::to_json_string(
  self : FetchPackagesResponse,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn FetchPackagesResponse::from_json_string(
  s : String,
) -> Result[FetchPackagesResponse, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match FetchPackagesResponse::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}

///|
pub(all) struct QueryOutlinesRequest {
  package : String
} derive(Eq)

///|
pub fn QueryOutlinesRequest::to_json(self : QueryOutlinesRequest) -> Json {
  let obj : Map[String, Json] = {}
  obj["package"] = Json::string(self.package)
  Json::object(obj)
}

///|
pub fn QueryOutlinesRequest::from_json(j : Json) -> QueryOutlinesRequest? {
  guard j is Object(obj) else { return None }
  let package : String = match obj.get("package") {
    Some(value) =>
      match
        (match value {
          String(text) => Some(text)
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => return None
  }
  Some({ package, })
}

///|
pub fn QueryOutlinesRequest::to_json_string(
  self : QueryOutlinesRequest,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn QueryOutlinesRequest::from_json_string(
  s : String,
) -> Result[QueryOutlinesRequest, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match QueryOutlinesRequest::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}

///|
pub(all) struct QueryOutlinesResponse {
  package : String
  outlines : Array[Outline]
} derive(Eq)

///|
pub fn QueryOutlinesResponse::to_json(self : QueryOutlinesResponse) -> Json {
  let obj : Map[String, Json] = {}
  obj["package"] = Json::string(self.package)
  obj["outlines"] = Json::array(self.outlines.map(fn(item) { item.to_json() }))
  Json::object(obj)
}

///|
pub fn QueryOutlinesResponse::from_json(j : Json) -> QueryOutlinesResponse? {
  guard j is Object(obj) else { return None }
  let package : String = match obj.get("package") {
    Some(value) =>
      match
        (match value {
          String(text) => Some(text)
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => return None
  }
  let outlines : Array[Outline] = match obj.get("outlines") {
    Some(value) =>
      match
        (match value {
          Array(items) => {
            let parsed : Array[Outline] = []
            for item in items {
              match Outline::from_json(item) {
                Some(value) => parsed.push(value)
                None => return None
              }
            }
            Some(parsed)
          }
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => []
  }
  Some({ package, outlines })
}

///|
pub fn QueryOutlinesResponse::to_json_string(
  self : QueryOutlinesResponse,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn QueryOutlinesResponse::from_json_string(
  s : String,
) -> Result[QueryOutlinesResponse, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match QueryOutlinesResponse::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}

///|
pub(all) struct QueryRequest {
  package : String
  exact_id : String?
  exact_symbol : String?
  text : String?
  limit : Int?
  cursor : String?
} derive(Eq)

///|
pub fn QueryRequest::to_json(self : QueryRequest) -> Json {
  let obj : Map[String, Json] = {}
  obj["package"] = Json::string(self.package)
  match self.exact_id {
    Some(value) => obj["exact_id"] = Json::string(value)
    None => ()
  }
  match self.exact_symbol {
    Some(value) => obj["exact_symbol"] = Json::string(value)
    None => ()
  }
  match self.text {
    Some(value) => obj["text"] = Json::string(value)
    None => ()
  }
  match self.limit {
    Some(value) =>
      obj["limit"] = Json::number(value.to_double(), repr="\{value}")
    None => ()
  }
  match self.cursor {
    Some(value) => obj["cursor"] = Json::string(value)
    None => ()
  }
  Json::object(obj)
}

///|
pub fn QueryRequest::from_json(j : Json) -> QueryRequest? {
  guard j is Object(obj) else { return None }
  let package : String = match obj.get("package") {
    Some(value) =>
      match
        (match value {
          String(text) => Some(text)
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => return None
  }
  let exact_id : String? = match obj.get("exact_id") {
    Some(value) =>
      match value {
        String(text) => Some(text)
        _ => None
      }
    None =>
      match obj.get("exactId") {
        Some(value) =>
          match value {
            String(text) => Some(text)
            _ => None
          }
        None => None
      }
  }
  let exact_symbol : String? = match obj.get("exact_symbol") {
    Some(value) =>
      match value {
        String(text) => Some(text)
        _ => None
      }
    None =>
      match obj.get("exactSymbol") {
        Some(value) =>
          match value {
            String(text) => Some(text)
            _ => None
          }
        None => None
      }
  }
  let text : String? = match obj.get("text") {
    Some(value) =>
      match value {
        String(text) => Some(text)
        _ => None
      }
    None => None
  }
  let limit : Int? = match obj.get("limit") {
    Some(value) =>
      match value {
        Number(v, ..) => Some(v.to_int())
        _ => None
      }
    None => None
  }
  let cursor : String? = match obj.get("cursor") {
    Some(value) =>
      match value {
        String(text) => Some(text)
        _ => None
      }
    None => None
  }
  Some({ package, exact_id, exact_symbol, text, limit, cursor })
}

///|
pub fn QueryRequest::to_json_string(
  self : QueryRequest,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn QueryRequest::from_json_string(
  s : String,
) -> Result[QueryRequest, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match QueryRequest::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}

///|
pub(all) struct QueryResponse {
  package : String
  mode : String
  matches : Array[QueryMatch]
  cursor : String?
} derive(Eq)

///|
pub fn QueryResponse::to_json(self : QueryResponse) -> Json {
  let obj : Map[String, Json] = {}
  obj["package"] = Json::string(self.package)
  obj["mode"] = Json::string(self.mode)
  obj["matches"] = Json::array(self.matches.map(fn(item) { item.to_json() }))
  match self.cursor {
    Some(value) => obj["cursor"] = Json::string(value)
    None => ()
  }
  Json::object(obj)
}

///|
pub fn QueryResponse::from_json(j : Json) -> QueryResponse? {
  guard j is Object(obj) else { return None }
  let package : String = match obj.get("package") {
    Some(value) =>
      match
        (match value {
          String(text) => Some(text)
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => return None
  }
  let mode : String = match obj.get("mode") {
    Some(value) =>
      match
        (match value {
          String(text) => Some(text)
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => return None
  }
  let matches : Array[QueryMatch] = match obj.get("matches") {
    Some(value) =>
      match
        (match value {
          Array(items) => {
            let parsed : Array[QueryMatch] = []
            for item in items {
              match QueryMatch::from_json(item) {
                Some(value) => parsed.push(value)
                None => return None
              }
            }
            Some(parsed)
          }
          _ => None
        }) {
        Some(parsed) => parsed
        None => return None
      }
    None => []
  }
  let cursor : String? = match obj.get("cursor") {
    Some(value) =>
      match value {
        String(text) => Some(text)
        _ => None
      }
    None => None
  }
  Some({ package, mode, matches, cursor })
}

///|
pub fn QueryResponse::to_json_string(
  self : QueryResponse,
) -> Result[String, String] {
  Ok(self.to_json().stringify())
}

///|
pub fn QueryResponse::from_json_string(
  s : String,
) -> Result[QueryResponse, String] {
  let parsed = try? @json.parse(s)
  guard parsed is Ok(value) else { return Err("invalid json") }
  match QueryResponse::from_json(value) {
    Some(out) => Ok(out)
    None => Err("type mismatch")
  }
}
