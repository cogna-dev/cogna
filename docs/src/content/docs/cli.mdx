---
title: CLI 参考
description: 当前 Cogna CLI 的真实命令面、默认行为，以及各命令会读取或生成哪些文件。
order: 6
---

# CLI 参考

当前版本的 Cogna CLI 以 `cogna.yml` 为中心：`build`、`diff`、`check`、`publish`、`registry serve`、`mcp serve` 都优先读取当前工作目录对应仓库的配置，而不是要求你在命令行里重复传路径。

---

## `cogna init`

生成一个新的 `cogna.yml` 模板：

```bash
cogna init
```

默认输出到当前目录的 `./cogna.yml`。

---

## `cogna build`

读取当前仓库的 `cogna.yml`，生成 `dist/` 下的 bundle 与索引产物：

```bash
cogna build
```

典型输出：

```text
dist/
├── manifest.json
├── declarations.ndjson
├── symbols.ndjson
├── software-components.ndjson
├── sbom.spdx.json
├── metadata.json
├── checksums.txt
├── bundle.ciq.tgz
└── dependencies/              ← 仅在可解析本地依赖时出现
```

如果 `profile: policy-bundle`，还会额外生成：

```text
dist/
├── opa-bundle.tar.gz
└── policy.rules.json
```

---

## `cogna diff`

根据当前仓库配置和 `dist/base` / `dist/target` 产物生成结构化 diff：

```bash
cogna diff
```

输出文件：

```text
dist/diff.json
```

---

## `cogna check`

读取 `cogna.yml` 里的 `checks.policy` 并对 `dist/diff.json` 执行 policy 检查：

```bash
cogna check
```

输出文件：

```text
dist/check.sarif.json
```

> `check` 依赖本机可用的 `opa eval`。

---

## `cogna query`

一次性查询某个 bundle 中的符号或 operation：

```bash
cogna query <query-file>
```

示例查询文件：

```json
{
  "schemaVersion": "ciq-query/v1",
  "purl": "pkg:cargo/tokio@1.43.0",
  "selector": {
    "path": "tokio::sync::Mutex::lock"
  },
  "intent": "upgrade"
}
```

`selector.kind` 当前是可选的；如果省略，Cogna 会在服务端做 ranked matching。

---

## `cogna publish`

发布当前仓库 `dist/bundle.ciq.tgz` 到本地或远端 registry：

```bash
cogna publish
```

输出文件：

```text
dist/publish.json
```

如果 `publish.includeDependencies: true`，并且 `build` 阶段生成了 dependency bundles，publish receipt 还会聚合：

- `dependencyCount`
- `dependencyReceipts`

---

## `cogna registry`

### `registry serve`

启动本地 HTTP registry server：

```bash
cogna registry serve --port 8787
```

默认端口来自 `cogna.yml -> registry.port`，命令行 `--port` 会覆盖它。

本地存储目录优先级：

1. `COGNA_REGISTRY_STORE_DIR`
2. `cogna.yml -> registry.storeDir`
3. `.cogna/cache/registry`

### `registry status`

查看 registry descriptor 和健康状态：

```bash
cogna registry status
```

---

## `cogna mcp`

### `mcp serve`

在前台运行 MCP server：

```bash
cogna mcp serve --port 3000
```

### `mcp status`

查看 MCP server descriptor 和健康状态：

```bash
cogna mcp status
```

---

## 当前推荐工作流

```bash
cogna init
cogna build
cogna diff
cogna check
cogna publish
cogna registry serve --port 8787
```

另一个终端里，如果要给 AI / Agent 使用：

```bash
cogna mcp serve --port 3000
```
