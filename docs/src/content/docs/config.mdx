---
title: 配置与 PURL
description: cogna.yml 的 v2 字段说明、默认值，以及 cache / SBOM / MCP 的配置方式。
order: 7
---

# 配置与 PURL

Cogna 默认读取仓库根目录的 `cogna.yml`。如果只有旧文件名 `cogna.yaml`，当前版本仍会兼容读取，但新的初始化、文档和示例都统一使用 `cogna.yml`。

## 一个完整的 v2 配置示例

```yaml
schemaVersion: ciq-config/v2
profile: openapi-spec
purl: pkg:openapi/acme/payment-api@2026.03.01
source:
  repo: https://github.com/acme/payment-api
  ref: main
inputs:
  include:
    - openapi/**/*.yaml
checks:
  format: sarif
  policy: ./dist/policy.ciq.tgz
diff:
  includeTestChanges: false
sbom:
  format: spdx
  dependencyBundles: true
  requireLocalPackages: true
cache:
  type: local
  local:
    storeDir: .cogna/cache
  http:
    baseUrl: http://127.0.0.1:8787
    timeoutMs: 10000
mcp:
  port: 3000
```

## 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `schemaVersion` | 是 | 当前写 `ciq-config/v2` |
| `profile` | 是 | 仓库类型：Go / Rust / Terraform / OpenAPI / policy |
| `purl` | 是 | 当前仓库或规格的唯一标识 |
| `source.repo` | 是 | 源仓库地址 |
| `source.ref` | 是 | 默认分支或基线引用 |
| `inputs.include` | 是 | 要扫描的文件 glob |
| `checks.format` | 否 | 目前固定为 `sarif` |
| `checks.policy` | 否 | `cogna check` 默认读取的 policy bundle 路径 |
| `diff.includeTestChanges` | 否 | `diff` 是否包含测试变化 |
| `sbom.format` | 否 | 当前仅支持 `spdx` |
| `sbom.dependencyBundles` | 否 | 是否为可解析的本地依赖额外产出 dependency bundle |
| `sbom.requireLocalPackages` | 否 | 本地依赖无法解析时是否直接失败 |
| `cache.type` | 否 | `local` 或 `http`；默认 `local` |
| `cache.local.storeDir` | 否 | 本地 cache 根目录 |
| `cache.http.baseUrl` | 否 | 远端 HTTP cache / proxy 地址 |
| `cache.http.timeoutMs` | 否 | HTTP cache 请求超时（毫秒） |
| `mcp.port` | 否 | `cogna mcp serve` 默认端口 |

## 配置优先级

### Local cache 目录

本地 cache 存储目录按下面顺序决定：

1. `cogna.yml` 中的 `cache.local.storeDir`
2. 默认值 `.cogna/cache`

### `cache.type=http` 的行为

当 `cache.type` 设置为 `http` 时，`cogna build` 会先检查本地 cache；如果本地 miss，就从 `cache.http.baseUrl` 指向的 HTTP cache 拉取对象并回填到本地 cache，再物化到 `dist/`。这让依赖结果可以通过远端复用，而不必重新构建。

```bash
cogna serve --port 8787
```

## 支持的 profile

| profile | 用途 |
|---------|------|
| `go-module` | Go module |
| `rust-crate` | Rust crate |
| `terraform-module` | Terraform module |
| `openapi-spec` | OpenAPI 规格仓库 |
| `policy-bundle` | OPA policy bundle |

## 什么是 PURL？

PURL（Package URL）是 Cogna 用来标识 bundle、diff 基线和 cache 索引记录的标准格式：

```text
pkg:<type>/<namespace>/<name>@<version>
```

示例：

```text
pkg:golang/github.com/gin-gonic/gin@1.10.0
pkg:cargo/tokio@1.43.0
pkg:terraform/hashicorp/terraform-aws-vpc@5.18.1
pkg:openapi/acme/payment-api@2026.03.01
pkg:generic/acme/compat-policy@2026.03.27
```

## 下一步

- 初始化并生成配置文件：[快速开始](/docs/quickstart)
- 查看命令面与当前 reality：[CLI 参考](/docs/cli)
- 理解 bundle / cache / MCP 如何串起来：[工作方式](/docs/runtime-model)
