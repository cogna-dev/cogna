---
title: MCP / Registry 参考
description: 当前 MCP 与 Registry 命令面、HTTP API，以及 Docker / storeDir 运行方式的精确参考。
order: 9
---

# MCP / Registry 参考

这里记录的是**当前 reality**：只有 `serve` / `status` 公开暴露给 CLI；上传、解析、下载 bundle 的行为通过 `publish` 和 registry HTTP API 完成。

---

## MCP 命令

| 命令 | 说明 |
|------|------|
| `cogna mcp serve [--port <port>]` | 在前台运行 MCP server |
| `cogna mcp status` | 查看 MCP descriptor 和健康状态 |

示例：

```bash
cogna build
cogna mcp serve --port 3000
```

---

## Registry 命令

| 命令 | 说明 |
|------|------|
| `cogna publish` | 发布当前仓库 `dist/bundle.ciq.tgz` |
| `cogna registry serve [--port <port>]` | 启动本地 HTTP registry server |
| `cogna registry status` | 查看 registry descriptor 和健康状态 |

### Registry store 目录优先级

1. `COGNA_REGISTRY_STORE_DIR`
2. `cogna.yml -> registry.storeDir`
3. `.cogna/cache/registry`

示例：

```bash
COGNA_REGISTRY_STORE_DIR=/data/registry cogna registry serve --port 8787
```

---

## HTTP Registry 接口

运行 `cogna registry serve` 后，可用的 API：

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/health` | 健康检查 |
| `POST` | `/api/v1/bundles` | 上传 bundle |
| `GET` | `/api/v1/bundles/resolve?purl=<purl>` | 按 PURL 查找 bundle |
| `GET` | `/api/v1/packages/{purl}/versions` | 列出某个包的已发布版本 |
| `GET` | `/api/v1/bundles/{bundleId}` | 获取 bundle 元数据 |
| `GET` | `/api/v1/bundles/{bundleId}/download` | 下载 bundle 文件 |

---

## Docker 运行 Registry

P6 完成后，registry server 可以直接以容器方式交付：

```bash
docker build -f integrations/deployments/docker/Dockerfile -t cogna-registry:local .

docker run --rm \
  -p 8787:8787 \
  -e COGNA_REGISTRY_STORE_DIR=/data/registry \
  -v "$PWD/.cogna-docker-registry:/data/registry" \
  cogna-registry:local
```

容器里 `COGNA_REGISTRY_STORE_DIR` 必须指向可写目录，通常配合 volume 持久化。

---

## 下一步

- 命令面总览：[CLI 参考](/docs/cli)
- MCP 使用方式：[AI 查询（MCP）](/docs/mcp)
- bundle / registry / MCP 如何连起来：[工作方式](/docs/runtime-model)
