---
title: 开发进度
description: Cogna 当前真实能力、当前 cleanup 阶段，以及计划与代码之间的 gap 对账。
order: 12
---

# 开发进度

> 最后更新时间：2026-04-17（本轮新增：`cogna-dev/mapi` 升级至 0.1.1，`justfile` 补充 `check-sdk` / `test-sdk` / `fmt-sdk` / `gen-registry` recipes 并更新 `ci` 依赖，`ci.yml` 新增 `sdk-check` job，`CONTRIBUTING.md` 能力矩阵同步更新。Asia/Manila，当前 active stage：A004 Stage C~E proto-first codegen integration。**本轮新增 build-release pipeline**：`.github/workflows/release.yml` 已创建，支持 push tag `v*` 自动触发多平台 CLI 构建（x86_64/aarch64 × Linux/macOS + x86_64-Windows）并挂载到 GitHub Release，SDK 同步通过 `cogna-dev/moonbit-actions/publish@v0` 发布到 mooncakes.io；`ci.yml` 新增 `pre-release-check` job（semver 格式校验 + `moon publish --dry-run`）；`justfile` 已重整为统一动词前缀风格（`build-*` / `test-*` / `check-*` / `install-*` / `gen-*` / `release-*`），包含能力矩阵注释与依赖图；`CONTRIBUTING.md` 已同步更新为基于 justfile 的完整贡献者指引；`integrations/actions/setup-moonbit/action.yml` 已恢复（此前被误删）。**本轮新增 registry transport / layout cleanup**：`src/async/` 已删除并由新的 `src/shell/` package 取代，原先分散在 `src/async` 的 process/http-client helper 现统一收口为 native-only shell adapter；registry 逻辑也已完全并回 `src/registry/` vertical，其中 `src/lib/registry/` 已删除、`src/registry/` 根目录不再保留 loose `.mbt` entry，仅保留 `client/` 与 `server/` 两个子 package；registry server 侧已切换为 `cogna-dev/mapi` router/app contract + 内联 HTTP accept loop，command packages 对共享 transport/registry import 也已同步改到 `@shell` / `@client`；仓库根目录新增 `openapi.json`，显式描述 `/health`、bundle upload/resolve/detail/download 与 package versions 共 6 条公开 registry API 路由。此前的 parser cleanup 也仍成立：`src/lib/parsec` 已删除；原先被其承载的轻量 string/signature helper 已收口到 `src/extractors/`，`src/lib/hcl` 现已改为**优先使用外部 `cogna-dev/x/hcl` 的 typed `Document` AST**，并在本地做 compatibility adapter，把结果恢复成 `HclFile` / `HclBlock` / `HclAttr` 供 Terraform extractor 继续消费；对于 `cogna-dev/x/hcl` 当前尚不接受的 Terraform 扩展表达式，仍保留 fallback 路径以维持既有行为。与此同时，`src/cmd/build/extractor.mbt` 中原本散落的 Terraform / OpenAPI 解析逻辑已分别迁入 `src/extractors/terraform/` 与 `src/extractors/openapi/`，`cmd/build` 只保留 orchestration / dispatch。`parse_hcl_file(lines)` 与 `HclFile` / `HclBlock` / `HclAttr` public API 仍保持不变，现有 HCL compatibility tests、extractor tests 与 build snapshot tests 已通过。此前的 schema cleanup 也仍成立：仓库内手写 `json-schema` / `ciq-schema` runtime 路线已删除，manifest 不再携带 `declarationSchema`，command/core/registry 的 trusted path 已改为轻量 shape check；未来如需格式校验，唯一方向收口为由 proto-gen 直接生成 validator/helper，而不是继续维护 JSON Schema 资产。另：已根据 `spec/api.md` 新增 `spec/ui.md`，并在 `integrations/desktop/` 实现基于 Electron + React 的全功能桌面 demo，本轮已完成 **二期 UI 升级**：无边框窗口（`frame: false` + `titleBarStyle: hidden` + macOS traffic-light 定位）、自定义 App Menubar（`-webkit-app-region: drag`）、Tailwind CSS v4 + Shadcn UI 组件替换原始 CSS、Command palette（⌘K / Ctrl+K，基于 Shadcn `CommandDialog`），以及完整的三栏 App 布局（sidebar + explorer + detail）；当前因 `integrations/sdk/node/src/moonbit.ts` 仍依赖 monorepo `_build/js/debug/build/sdk/sdk.js` 路径，desktop 侧保持 mock-first 数据层，同时保留 `Build` / `Diff` 的 CLI guidance 卡片。`pnpm typecheck`、`pnpm lint` 与 `pnpm build` 已通过。）
>
> 本页是唯一的 progress source of truth。`/contrib/progress` 仅保留兼容入口，不再维护独立事实。

## 当前产品状态

Cogna 当前仍以 **local-only / config-driven** 方式工作：围绕 `cogna.yml`、本地 bundle、本地 registry store、MCP 与 SARIF 输出形成单机闭环。A003 的 benchmark / parity 与 boundary cleanup 已完成，native-only async 收口也已经落地；A004 的 JS SDK/CLI 边界第一批实现也已落地：`src/sdk/contracts/`、`src/sdk/index/`、`src/sdk/query/` 已创建，`cmd/query` / `cmd/build` / `cmd/mcp/core` 已完成对 SDK core 的薄封装或委托，`integrations/sdk/node/` 也已收口为带稳定 wrapper API 的 `@cogna/sdk`，同时 repo 根目录已补 `pnpm-workspace.yaml`，公开 TS 类型已从大量 `unknown` 收紧为 CIQ/local-index 相关结构。在此基础上，当前主线工作已切到：#strong[A004 proto-only 丰富 contract review]。本轮继续在五文件布局内重构 proto：共享文件保持为 `bundle.proto`，而 `query.proto` 已开始从旧的 selector/result 模型转向“Bundle 虚拟二维表 + SQL”模型，用 `BuiltinTable`、`TableSchema`、`ColumnSchema`、`QueryValue`、`QueryResultSet` 和 `QueryCatalog` 来定义可查询的关系视图，减少后续继续堆叠 `List/Get` 接口的需要。

| 能力 | 当前状态 |
|------|----------|
| 构建代码仓库 / API 规格 bundle | ✅ 已支持 |
| 版本对比与结构化 diff | ✅ 已支持（Go / Rust / Terraform / OpenAPI） |
| policy 检查与 SARIF 输出 | ✅ 已支持 |
| CLI / MCP 查询 bundle 内容 | ✅ 已支持 |
| native-vs-lsp parity | ✅ 已支持（同一 canonical 输出上零差异） |
| 真实 benchmark reporting | ✅ 已支持（wall/cpu/RSS 均为 Python + `psutil` 实测） |
| boundary cleanup | ✅ 已完成 |

## 当前命令面（真实代码）

- `cogna init`
- `cogna build`
- `cogna diff`
- `cogna check`
- `cogna query <query-file>`
- `cogna publish`
- `cogna registry serve|status`
- `cogna mcp serve|status`

## 当前 cleanup 计划对账总览

| 计划项 | 计划目标 | 当前代码 / 证据 | 状态 | 仍有 gap |
|--------|----------|------------------|------|----------|
| C0 | truth reset | `spec/plan.typ` 已改为 `lib = reusable only` 的 cleanup-first 计划；本页已切到 boundary cleanup 主线 | ✅ 已完成 | 后续只需继续让 active stage 与实际迁移同步 |
| C1 | repo-noise cleanup | 已清掉当前分支中仍被追踪且命中 `.gitignore` 规则的顶层 `pkg.generated.mbti`；其余 `examples/*/snapshots/cogna/*` 当前仍以 fixture 形态存在，需与后续 fixture policy 一并收口 | ✅ 已完成 | 后续只需在更大清理阶段继续统一 fixture / generated policy |
| C2 | `sbom -> src/sbom` | 已进一步完成反向收口：原本本地 `src/sbom/**` 已删除；dependency snapshot 能力现已继续收口到 `src/core/snapshot/**`；`src/cmd/build` 现改用外部 `cogna-dev/sbom@0.1.0` + 本地 snapshot helper | ✅ 已完成 | SPDX 产物现为外部库生成的 SPDX 2.3 JSON，而非旧的 SPDX 3.0 JSON-LD |
| C3 | `policy_catalog -> src/policy` | `src/lib/policy_catalog/**` 已迁到 `src/policy/**`，且 `build` / `check` / `scripts/gendoc` 已改用新包路径 | ✅ 已完成 | 后续只需随 README / docs 统一收口 |
| C4 | `extract -> src/extractors` | `src/lib/extract/**` 先迁到 `src/extractors/**`，本轮进一步把共享 record/text/state/emit helper 收口到 `src/core/extract/**`；`src/extractors/` 现只保留 language-specific subpackages，`src/cmd/build` 与各 extractor package 已同步改用新包路径 | ✅ 已完成 | 后续只需继续按更大阶段处理 Terraform/OpenAPI 与 runtime wrapper 收口 |
| C5 | runtime wrapper 删除 | 已新增 `src/async/` 作为共享 native-only host adapter；`registry` / `publish` / `mcp` / `check` 已收口为 native-only；旧 runtime/shell wrapper 已删除；CLI/Docker integration entrypoint 已迁到 `integrations/`，并补上 detached spawn stdio 收口避免 native e2e hang | ✅ 已完成 | 后续只需在日常维护中继续守住 native-only 验证与 entrypoint 路径一致性 |
| C6 | 删除 `src/native` | `extract_native()` 已收口为直接调用 in-repo MoonBit extractor（`collect_records()`）；`src/native/` 已删除；bench/e2e 的 `COGNA_NATIVE_*` override 路径已全部移除 | ✅ 已完成 | 后续只需继续保持 native author path 与 parity 验证稳定 |
| C7 | README / warning / 待提交整理 | `src/` 与 `src/lib/` 的独立顶层 package README 已补齐；mixed-target warning 已通过 target-specific package、`supported_targets = "native"` 与安全 stub 引用收口；`src/cmd/**/tmp_*` 写路径已全部迁到 `/tmp/cogna-*`；旧 tracked generated artifacts 已清除 | ✅ 已完成 | — |

## 已完成摘要（summary-only）

- A003 已从旧 helper-binary / dual-snapshot 叙事收口为 single canonical output；
- `src/e2e/bench.py` 已成为真实 benchmark harness，wall / CPU / RSS 均来自 Python + `psutil` 的子进程树实测；
- `spec/A003-benchmark-report.typ` 已改为 `native vs lsp` 的真实时间 / CPU / 内存 / diff 对照；
- Go native helper 已为 `--files` 场景加入 selected-file AST fast path，phase-5 benchmark 中 native 已反超 lsp；
- `src/lib/parsec` 已删除；原本挂在其中的 extractor text helper 已先迁回 `src/extractors/`，本轮又进一步收口到 `src/core/extract/`；`src/lib/hcl` 当前改为直接依赖外部 `cogna-dev/parkit/nom`；同时 `src/cmd/build/extractor.mbt` 中的 Terraform / OpenAPI 逻辑已下沉到 `src/extractors/terraform/` 与 `src/extractors/openapi/`，build 入口改为 thin dispatch；MCP server 现直接由 `src/cmd/mcp/core` 基于外部 `cogna-dev/mcp-sdk` 装配，`src/lib/` 的复用库边界比之前更清晰；
- `src/` 下遗留的 `derive(Show)` deprecated syntax 已全部收口为显式 `Show` impl，并补齐了 command / policy / sbom / e2e helper 等本轮触达包的调试输出；
- runtime wrapper cleanup 已进一步完成：`src/lib/shell/` 与 `src/lib/server-runtime/` 已删除，相关 feature slice 直接改用 `moonbitlang/async`、`moonbitlang/async/process`、`moonbitlang/async/http` 与 `moonbitlang/async/socket`；
- registry transport/layout cleanup 已完成：`src/async/` 与 `src/lib/registry/` 均已删除；新的 shared native-only transport adapter 位于 `src/shell/`；`src/registry/` 根目录已去掉 loose `.mbt` / `moon.pkg`，只保留 `client/` 与 `server/` 子 package；server 端 HTTP surface 现由 `cogna-dev/mapi` contract + 内联 accept loop 承载，repo 根目录也已补充 6-route `openapi.json`；
- 仓库卫生清理已继续推进：已删除 tracked `src/**/pkg.generated.mbti`、`src/native/rust/target/**`、`src/native/go/go` 与 `src/cmd/registry/tmp_*`；当前 native 语义已完全收口到 in-repo MoonBit extractor，`src/native/` 目录本身也已删除。
- C6 / C7 已完成：`extract_native()` 已重写为直接调用 in-repo MoonBit extractor（`collect_records()`），不再依赖任何外部 helper binary；native-only e2e 已拆到 `src/e2e/native/` 与 `src/e2e/review/native/`；此前的 `warn-list: "-29"` stopgap 已收口，`moon check --target wasm` / `moon check --target native`、`moon test --target wasm` / `moon test --target native`、`moon build --target native`、`pnpm build` 与 Typst compile 已全部通过。
- `src/lib/hcl` 已从本地 `src/lib/parsec` line-combinator 实现进一步切到外部 `cogna-dev/x/hcl` 的 typed `Document` AST 作为主解析路径，并在本地 adapter 中补回 `line_no` / `signature` / immediate-attrs 兼容语义；对 `cogna-dev/x/hcl` 当前尚不接受的 Terraform 扩展表达式则保留 fallback 路径。`parse_hcl_file(lines)` 与 `HclFile` / `HclBlock` / `HclAttr` public API 保持不变，`moon test src/lib/hcl/parse_test.mbt --target native`、`moon test src/extractors --target native` 与 `moon test src/cmd/build/snapshot_test.mbt --target native` 已通过。
- `spec/design.typ` 已补充为当前实现导向的设计说明草案：收口了当前 CLI surface、`cogna.yml` / `ciq-config/v2`、SPDX-first bundle 产物、`ciq-opa-bundle/v1`、registry `bundleId` / `downloadUrl` / `ETag` 语义，以及 MCP `serve|status` / `/rpc` / `/health` / tool surface 等真实 contract。
- A004 JS SDK / CLI 边界第一批实现已落地：`integrations/sdk/node/` 已替换错误的 `integration/sdk/node/` 路径，repo 根目录已补 `pnpm-workspace.yaml`，`@cogna/sdk` 对外 `.d.ts` 中的大量 `unknown` 已收紧为 CIQ/local-index 相关结构，且 `node integrations/sdk/node/scripts/build.mjs`、Node smoke、`npm pack --dry-run`、`moon check src/sdk/contracts src/sdk/index src/sdk/query --target js` 与 `moon test src/cmd/query/main_test.mbt --target wasm` 均已通过。
- A004 当前主线仍为 proto-only 五文件 contract，但 `Query` 已进入第二轮重构：不再继续围绕 selector/outlines 的专用接口增长，而是把 bundle 抽象成虚拟二维表，并通过 SQL 风格的 `QueryInput.sql` / `QueryResultSet` / `QueryCatalog` 暴露统一查询面。
- SBOM 迁移已完成：`src/sbom/` 本地实现已删除；dependency bundle 所需的文件清单/摘要能力已从早期的 `src/lib/snapshot/` 继续收口到 `src/core/snapshot/`；`src/cmd/build` 已切换到外部 `cogna-dev/sbom@0.1.0` 扫描与 SPDX 导出，同时保留 Cogna 自定义 NDJSON schema。
- desktop review demo 已落地：`spec/ui.md` 现已收口 `Build` / `Diff` / `FetchPackages` / `QueryOutlines` / `Query` 的桌面信息架构；`integrations/desktop/` 已从默认欢迎页替换为三栏 API explorer，包含 package tree、outlines/query pane、detail pane，以及基于 `pnpm exec cogna ...` 的 CLI guidance；由于当前 Node SDK 仍绑定 monorepo `_build` runtime，demo 暂以 mock-first 数据层交付。

## 当前 active stage

当前 active stage 是 #strong[`A004 proto-first codegen integration（Stage E SDK runtime facade 收口）`]。proto-only 五文件 contract 已基本稳定，当前重点已从“继续改 proto 形状”转向“把自定义 Go plugin 生成链路接入真实构建/发布面”：`buf.gen.yaml` 作为唯一生成入口，`src/sdk/proto/xaclabs/cogna/v1` 作为 MoonBit DTO 包，`integrations/sdk/node/dist/generated/*` 作为 npm 发布物中的 proto DTO/JSON helper 子路径；同时本轮已把 Node SDK 从 String→String JSON wrapper 改为直接绑定编译后的 MoonBit JS export，并通过 TS facade 复用 `generated/*.ts` 类型定义，实现无 JSON 序列化的强类型调用。配套地，旧的手写 schema/runtime schema 路线已经删除，后续若需要结构校验，应直接从 proto 生成 validator/helper。

### 当前阶段目标

收口 Stage C（MoonBit target）、Stage D（Node TS target）与 Stage E（Packaging / SDK surface integration）的已实现部分：确保主 `buf.gen.yaml` 直接驱动本地 plugin；把 MoonBit DTO 包收口到 `src/sdk/proto/**`，让 Node build 把 `src/generated/*.ts` 编译为 `dist/generated/*.js` / `.d.ts`；并补一个带少量第三方依赖的标准 fixture 项目 `examples/sdk-acceptance/full/repo` 与 native e2e `src/e2e/native/sdk_test.mbt`，验证 MoonBit/Node SDK 集成面。

### 当前阶段执行清单

- [x] 已完成：`buf.gen.yaml` 继续作为唯一 codegen truth source，MoonBit DTO 保持在 `src/sdk/proto/**`，Node DTO 保持在 `integrations/sdk/node/src/generated/*.ts`；
- [x] 已完成：`integrations/sdk/node/package.json` 已保留 `@cogna/sdk/generated` export，Node consumer 可直接获取 proto 生成的 DTO / parser / serializer；
- [x] 已完成：MoonBit JS target 现已从 String→String JSON wrapper 收口为直接导出真实 SDK API（`build` / `diff` / `fetch_packages` / `query_outlines` / `query`）；`src/sdk/js.mbt` 与 `src/sdk/moon.pkg` 已改为 direct export，并通过 `moon check --target js` / `moon build --target js`；
- [x] 已完成：`integrations/sdk/node/src/moonbit.ts` 现已改为 direct MoonBit runtime facade，不再执行 `JSON.stringify` / `JSON.parse`；Node 侧继续复用 `generated/*.ts` 的 TS 类型与 parse 函数作为结构化 guard；
- [x] 已完成：Node SDK 测试已新增 direct facade 覆盖，验证编译后的 MoonBit JS export 可直接返回 typed object；`npm test` 当前为 35/35 通过，`npx tsc --noEmit` 通过；
- [x] 已完成：MoonBit SDK native 测试仍保持通过，`moon test --target native -p cogna-dev/cogna/sdk` 当前 21/21 通过；
- [x] 已完成：旧 `src/lib/json-schema/`、`src/lib/ciq-schema/`、`src/config/schema*.mbt` 与 proto `moonbit-schema` emitter 已删除；manifest fixture / snapshot 中的 `declarationSchema` 已同步移除，registry client 现改为轻量 shape predicate；
- [x] 已完成：`spec/ui.md` 已新增桌面 UI 设计草案；`integrations/desktop/` 已实现 mock-first 的 Shadcn 风格 Electron demo，并通过 `pnpm typecheck`、`pnpm lint` 与 `pnpm build`；
- [ ] 当前剩余 gap：`src/core/api/` 中的部分函数目前仍是 stub / partial stub，实现行为与 fixture 语义尚未完全对齐；direct JS facade 已打通，但底层业务逻辑尚未与 `src/cmd/` 的真实实现完全联通。

## 其他阶段（summary-only）

| 阶段 | 目标 | 进入前提 |
|------|------|----------|
| C0 | truth reset | ✅ 已完成 |
| C1 | repo-noise cleanup | ✅ 已完成 |
| C2 | `sbom -> src/sbom` | ✅ 已完成 |
| C3 | `policy_catalog -> src/policy` | ✅ 已完成 |
| C4 | `extract -> src/extractors` | ✅ 已完成 |
| C5 | runtime wrapper 删除 | ✅ 已完成 |
| C6 | 删除 `src/native` | ✅ 已完成 |
| C7 | README / warning / 待提交整理 | ✅ 已完成 |

## 需要注意的当前 reality

- `policy_catalog` 当前只服务本仓库的 build/check 与 docs 生成脚本，不适合作为 `src/lib/` 包继续存在；
- `policy` / `snapshot` / shared extract helper / build-diff-query facade 已进一步完成 domain 收口：当前统一位于 `src/core/policy/`、`src/core/snapshot/`、`src/core/extract/`、`src/core/api/`；`src/core/` 根目录不再保留 loose `.mbt` / `moon.pkg`；
- `src/extractors/` 现只保留 `go/`、`rust/`、`terraform/`、`openapi/` 四个 language-specific subpackages；共享 helper 已迁到 `src/core/extract/`，且 `src/cmd/build` 与各 extractor package 已切换到新包路径；
- `src/cmd/build/extractor.mbt` 中一批纯转发 wrapper 已移除，Terraform / OpenAPI 解析逻辑也已迁到 `src/extractors/terraform/` 与 `src/extractors/openapi/`；当前保留的主要是 orchestration、policy extraction 与 backend probe；
- 当前分支里已确认并删除一批 tracked repo-noise：`src/**/pkg.generated.mbti`、`src/native/rust/target/**`、`src/native/go/go` 与 `src/cmd/registry/tmp_*`；`examples/*/snapshots/cogna/*` 仍按 fixture / baseline 处理，当前不应盲删；
- `src/sbom/` 已删除；dependency bundle 所需的文件清单与内容摘要能力现在位于 `src/core/snapshot/`；`src/cmd/build` 的 SBOM 扫描与 SPDX 导出则改由外部 `cogna-dev/sbom` 提供；
- `src/lib/` 下旧的 reusable parser shim 已进一步收口：`src/lib/hcl` 已删除，Terraform extractor 现直接依赖 `cogna-dev/x/hcl@0.1.2`；`registry` 已整体迁回 `src/registry/client` + `src/registry/server` vertical，`src/lib/registry/` 已删除；`parsec` 已删除，手写 `json-schema` / `ciq-schema` 路线也已删除，不再作为仓库内复用库维护；
- Terraform extractor 现直接消费 `cogna-dev/x/hcl@0.1.2` 的 typed AST 与 `Position { line, col }`，不再经过本地 `HclFile` adapter；canonical terraform snapshot baseline 已同步刷新，location / source_refs 现在会携带 upstream column metadata；
- `src/` 下原有 `derive(Show)` deprecated warning 已全部清掉；此前 mixed-target 包上的 `warn-list: "-29"` stopgap 也已收口：当前策略是按需要拆 target-specific package、对 truly native-only 包使用 `supported_targets = "native"`，并在 wasm/js stub 中保留安全的 import liveness；
- `moon test src/lib/hcl src/lib/registry` 与 `moon test src/cmd/mcp/core --target native` / `moon test src/cmd/mcp/main_test.mbt --target native` 已通过；MCP 相关逻辑现以标准 `/mcp` HTTP/SSE server 为准，由 `cmd/mcp/core` 的装配层测试覆盖；
- registry / command transport 现统一通过 `src/shell/` 共享 package 暴露；`cmd/check`、`cmd/mcp`、`cmd/registry`、`cmd/build` 与 `bench` 已改用该 shared adapter，旧 `src/async/` 已删除；
- C6 已完成：`src/native/` 已删除，`extract_native()` 现在直接调用 in-repo MoonBit extractor（`collect_records()`），不再有任何外部 binary 依赖；bench / e2e parity 仍然通过，因为 native 与 canonical 路径均调用同一 `collect_records()` 实现；`bench_wbtest.mbt` 已移除 `COGNA_NATIVE_*` override 逻辑；
- `examples/*/snapshots/cogna/*` 当前是 snapshot test baseline，而不是本轮 C1 可盲删的 repo-noise；是否继续保留这组 fixture 需要与更大的 baseline policy 一并决定。
- `moon check src/cmd/build`、`moon test src/cmd/build`、`moon test src/extractors`、`moon test src/e2e/extractor_parity_test.mbt`、`moon test src/e2e/lsp_extractor_test.mbt` 已通过；本轮 Terraform / OpenAPI extractor package split 之后，canonical snapshot baselines 也已同步刷新；
- `moon check src/registry`、`moon check src/lib/registry`、`moon check src/cmd/registry`、`moon check src/e2e`、`moon check src/e2e/native`、`moon test src/cmd/registry --target wasm`、`moon test --target native` 已通过，说明当前 registry runtime slice 与 native-only e2e split 后行为保持稳定；
- `moon check src/cmd/mcp/core src/cmd/mcp --target native`、`moon test src/cmd/mcp/core --target native` 与 `moon test src/cmd/mcp/main_test.mbt --target native` 已通过；当前 MCP 面已切到标准 `/mcp` endpoint 与 `streamable-http` descriptor 语义；
- `moon check src/cmd/registry/shell`、`moon check src/cmd/registry`、`moon test src/cmd/registry --target wasm` 已通过，说明 registry shell slice 行为保持稳定；
- `moon check src/cmd/check/shell`、`moon check src/cmd/check`、`moon test src/cmd/check` 已通过，说明 check shell slice 行为保持稳定；
- `moon check src/cmd/build/shell`、`moon check src/cmd/build`、`moon test src/cmd/build` 已通过，说明 build shell slice 行为保持稳定；
- `moon check src/e2e/shell`、`moon check src/e2e`、`moon check src/e2e/native`、`moon check src/e2e/review/native`、`moon test --target wasm`、`moon test --target native` 已通过，说明 portable / native-only e2e package split 后行为保持稳定；
- `moon check src/scripts/benchmark/shell`、`moon check src/scripts/benchmark`、`moon test src/scripts/benchmark --target native` 已通过，说明 benchmark script shell slice 行为保持稳定；
- `scripts/benchmark` 的 wbtest 已移除 `configure_native_engine_override` / `clear_native_engine_override`；benchmark harness 不再有 helper binary probe 路径；`moon check --target native` 0 error，`moon test src/cmd/build` 22/22 通过（C6 验证）；
- 本轮结构收口后需重新验证 `src/core/snapshot` / `src/core/policy` / `src/core/extract` / `src/core/api` 与相关 command packages；验证完成后再更新这里的通过命令；
- `moon test --target wasm`、`moon test --target native`、`moon build --target native`、`pnpm build` 与 `typst compile spec/plan.typ` / `typst compile spec/A003-native-extractor.typ` 已通过，说明本轮 generated cleanup、warning 收口与 docs/spec 同步未破坏行为。
- A004 MoonBit 侧第一批已落地：`src/sdk/contracts/`、`src/sdk/index/`、`src/sdk/query/` 已创建；`cmd/query` 已重构为 `@sdk_query` 薄封装（10/10 wasm tests 通过）；`cmd/build` 的 `extract_canonical` / `extract_native` 已委托 `@sdk_index`，`profile_mode_native` 已从 `cmd/build` 删除（21/21 native tests 通过）。
