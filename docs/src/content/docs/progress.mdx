---
title: 开发进度看板
description: 与 spec/plan.typ 对齐的当前 release ledger、验证状态与下一步执行重点。
order: 6
---

# CodeIQ 开发进度看板

> 最后更新时间：2026-03-24（Asia/Shanghai）

此页面只保留当前有效状态，并与 [`spec/plan.typ`](https://github.com/yufeiminds/codeiq/blob/main/spec/plan.typ) 同步：当前 roadmap 已重新排序，先完成主业务流程与单机 registry 基线，再优先关闭 provider 能力矩阵覆盖，最后再处理 trust / deployment / security。

## 当前总览（唯一状态面板）

| Release | 范围 | 当前状态 | 说明 |
| --- | --- | --- | --- |
| R1 | Local Governance Baseline | ✅ Completed | 本地治理 baseline 已冻结 |
| R2 | Extractor v1 Trustworthy | ✅ Completed | extractor v1 baseline 已冻结，Terraform 已转向 `lib/hcl` |
| R3 | Distribution Beta（repo-local / inproc） | ✅ Completed | repo-local / inproc 分发 beta 已形成可回归闭环 |
| R4 | Externalization Hardening | ✅ Completed | process lifecycle hardening 已交付 |
| R5 | Core Workflow Complete | ✅ Completed | 主业务流程闭环已完成并完成收口 |
| R6 | Single-Machine Registry Baseline | ✅ Completed | 单机/免认证 registry + publish baseline 已完成并关闭 |
| R7 | Provider Matrix Coverage | ✅ Completed | Go / Rust / Terraform / OpenAPI provider 证据面已完成收口 |
| R8 | Registry Trust & Verifier | ⚪ Planned | Provider matrix 收口后再处理 signature path、shared verifier 与 trust 议题 |
| R9 | Deployment & Security Hardening | ⚪ Planned | 最后处理部署、auth、security、ops 与企业级 hardening |

## 已完成阶段（摘要）

- `R1-R6` 已完成并冻结为当前 repo 基线；
- `R5` 已完成，主业务流程 `init → build → diff → check → query → MCP review` 已形成稳定闭环；
- `R6` 已完成，单机/免认证 registry + publish baseline 已关闭；
- 当前看板记录最新完成阶段与下一阶段方向：`R7` 已完成，下一步进入 `R8`。

## R7 · Provider Matrix Coverage（Completed）

**Scope**

- 把 `spec/A001-lsp-extractor.typ` 中列出的 provider 能力面尽量冻结为 repo 证据：
  - Go：package doc、embedded field、generic type/function、deprecated / 注释样本；
  - Rust：独立 doc comment 样本；
  - Terraform：data source、复杂 object type；
  - OpenAPI：path、discriminator、reference normalization。

**Done when**

- `examples/*/full/repo/*` 对四类 provider 的完整能力样本与 `examples/*/full/snapshots/lsp/*` 对齐；
- `src/cmd/build/extractor.mbt`、`src/lib/hcl/parse.mbt` 与 tests 能稳定产出新的 provider records；
- plan / progress / providers docs 对当前 active release 与验证命令保持一致。

**Verify**

- `moon check`
- `moon test src/cmd/build/snapshot_test.mbt`
- `moon test src/e2e/lsp_extractor_test.mbt`
- `pnpm build`（在 `docs/` 下）

**Out of scope**

- shared verifier、auth、hosted deployment、KMS/key rotation。

## R7 执行清单（唯一执行清单）

### R7-1 · Provider fixture / snapshot closure

**状态：✅ Completed**

- **Goal**：补齐 examples 语料与 snapshots，让 provider matrix 的关键缺口都有 repo 级证据。
- **Main touchpoints**：`examples/go-module/full/repo/main.go`、`examples/rust-crate/full/repo/lib.rs`、`examples/terraform-module/full/repo/main.tf`、`examples/openapi-spec/full/repo/openapi/payment.yaml`、`examples/*/full/snapshots/lsp/*`
- **Depends on**：`R6`
- **Done when**：
  - Go snapshot 覆盖 package doc、embedded field、generic type/function、deprecated / 注释样本；
  - Rust snapshot 覆盖独立 doc comment；
  - Terraform snapshot 覆盖 data source 与 object type；
  - OpenAPI snapshot 覆盖 path 与 discriminator。
- **Verify**：
  - `moon test src/cmd/build/snapshot_test.mbt`
- **Out of scope**：原生 `CodeIQ Extractor` 交叉验证实现。

### R7-2 · Extractor implementation closure

**状态：✅ Completed**

- **Goal**：让 build-time extractor 与 parser-first/provider-specific 路径真实产出 provider matrix 所需 records，而不是只在 docs 中声明。
- **Main touchpoints**：`src/cmd/build/extractor.mbt`、`src/lib/hcl/parse.mbt`、`src/e2e/lsp_extractor_test.mbt`
- **Depends on**：`R7-1`
- **Done when**：
  - Terraform inline data block 能被 parser-first 路径提取；
  - OpenAPI path / discriminator / `$ref` normalization 能稳定落盘；
  - Go field / embedded field 与 Rust doc comment 样本能稳定进入 bundle records。
- **Verify**：
  - `moon check`
  - `moon test src/e2e/lsp_extractor_test.mbt`
- **Out of scope**：LSP runtime productization、shared verifier。

### R7-3 · Provider docs / dashboard / release alignment

**状态：✅ Completed**

- **Goal**：让 `providers/progress/plan` 对 active release、provider coverage 现状和验证矩阵保持一致。
- **Main touchpoints**：`docs/src/content/docs/providers.mdx`、`docs/src/content/docs/progress.mdx`、`spec/plan.typ`
- **Depends on**：`R7-1`、`R7-2`
- **Done when**：
  - 文档把 provider matrix 作为当前 active release 明确写出；
  - progress / plan 与 provider docs 对 release 顺序、验证命令和下一步保持一致；
  - docs build 继续通过。
- **Verify**：
  - `pnpm build`（在 `docs/` 下）
- **Out of scope**：同时展开 `R8+` 的 trust / deployment 实施细节。

## 当前验证状态（R7相关）

以下结果是当前 `R7` provider matrix 可继续复用的基线证据：

| 验证项 | 结果 | 备注 |
| --- | --- | --- |
| `moon test src/cmd/build/snapshot_test.mbt` | ✅ 通过（本轮） | 覆盖四类 provider snapshots 与 metadata |
| `moon test src/e2e/lsp_extractor_test.mbt` | ✅ 通过（本轮） | 新增 Go / Rust / Terraform / OpenAPI provider evidence assertions |
| `moon check` | ✅ 通过（本轮） | 当前为 warnings only（0 errors） |
| `pnpm build`（`docs/`） | ✅ 通过（本轮） | 当前文档站可构建 |

**环境说明**

- 当前大图 native 测试仍会触发 MoonBit 编译器 ICE（`Invalid_argument("Moonc.Basic_lst.iter2")`），但本轮 provider coverage 验证主要落在 build snapshot / e2e extractor 路径；
- `moon check` 仍为当前 MoonBit 代码修改的主错误门槛，warnings 为既有基线。

## 非当前执行项（只保留方向）

- `R8`：处理 signature path、shared verifier feasibility、trust 模型；
- `R9`：处理 deployment、auth、security、ops 与企业级 hardening；
- 它们不是当前 release 的执行清单，不在本页继续展开。

## 本轮补充文档

- `docs/src/content/docs/providers.mdx`：已更新 provider 覆盖矩阵，纳入 Go/Rust/Terraform/OpenAPI 新证据；
- `docs/src/content/docs/progress.mdx`：已完成 provider matrix dashboard 收口；
- `spec/plan.typ`：已把下一阶段从 trust/verifier 调整为 provider matrix coverage，并完成本轮收口。

## 下一步（按 R7 收口任务执行）

1. 进入 `R8`：处理 signature path、shared verifier feasibility 与 trust 议题。
2. 在 `R8` 开始实现前，先保留并跟踪当前 MoonBit native `link-core` ICE 的独立最小 repro：`repro/moonbit-link-core-ice/`。
3. 当前已确认最小触发条件为 native blackbox test 中的 live `@zstd.compress(b"")` 调用；`plain_control` / `zstd import-only` / `dead declaration` / normal `build --target native` 均为对照通过。
4. 在 `R8` 完成后进入 `R9`：处理 deployment / auth / security / ops 收口。
5. 继续保持 MoonBit 工具链回归跟踪，待上游修复后恢复更多 native/js 全量回归。

## 相关文档

- [概览](/docs/introduction)
- [快速开始](/docs/quickstart)
- [索引与紧凑上下文](/docs/indexing)
- [MCP 工作流](/docs/mcp)
- [CLI 参考](/docs/cli)
- [Provider 覆盖率报告](/docs/providers)
